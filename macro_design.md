# QuickMenu マクロシステム 設計草案 (v0.2)

> 日付: 2026-08-07
> ステータス: 草案 (plan.md §2.8 / todo.md 低優先度 の詳細化)
> 決定済み: エディタはブロック+テキストのハイブリッド構成 (2026-08-07)

## 実装状況

- [x] **2026-08-07**: マクロ全体の前に、最小要素の `sleep` アクションを先行実装。
  - `SleepActionData` (type `"sleep"`, ティック指定, 1/20秒単位) を追加。
  - 複数ティックに跨る順次実行のため tick 駆動の `ActionExecutor` を追加 (待機中のアクションは次ティックで再開)。
  - `ActionPickerUI` → 「待機」追加、`ActionListEditorUI` に秒数入力欄。現行UIのまま追加可能。
- [x] **2026-08-07**: キーアクションを tap / press / release の3モードに対応。
  - `KeybindActionData` に `KeyPressMode` (`tap`/`press`/`release`) を追加。`press` はキーを押しっぱなしに、`release` は解放する。
  - `KeybindHandler` に保持キー管理 (`heldKeys`) を追加。実行終了・キャンセル時に自動解放し、キーが押されたまま残る事故を防止。
  - 直列化は `["key", "keybind名", "press"]` の3要素目にモードを保存。旧データは `tap` として互換読込。
  - **修正**: メニューを閉じる等のマウスグラブ切替時にバニラの `KeyMapping.setAll()` が実キー入力で `isDown` を上書きし press が消えていた。
    `KeyMappingHeldKeysMixin` (setAll の RETURN に注入) + 毎tickの再適用で、保留中のキーを維持するように修正。

---

## 1. 目的

ボタン1つ(またはキーバインド)で、複数コマンド・キー押下・待機・分岐・ループを組み合わせた処理を実行できるマクロ機能を追加する。

既存の `CommandActionData` / `KeybindActionData` の複数実行(順次)だけでは不可能な
「**待機を挟んだ処理**」「**条件分岐**」「**繰り返し**」を実現する。

---

## 2. 現状の課題

- 現在の実行モデルは**同期式**:
  `ActionButtonData.run()` (`src/main/kotlin/xyz/inorganic/quickmenu/data/ActionButtonData.kt:66-77`) が
  `actions.forEach { it.run() }` を**1ティック内**で全て実行して即終了する。
- このままでは `wait` / `sleep` / ループ制御は**動かない**。
  待機中もゲームを止めず複数ティックに跨って実行が進む**ティック駆動の協調スケジューラ**が必須。

---

## 3. 実行モデル(ティック駆動スケジューラ)

- 新規 `MacroExecutor`(シングルトン)を追加。**全クライアントティック**で進行中インスタンスを1ステップずつ前進させる。
- 各インスタンス(`MacroInstance`)の状態:
  - プログラムカウンタ(PC) … 次に実行する文の位置
  - `wait残りティック` … `wait` で停止中の残り時間
  - ループ/分岐/ラベルの制御状態
- 実行の流れ:
  1. `wait` 中 → 残りティックを減らすだけ。0 になったら再開。
  2. それ以外 → 次の `wait` か `exit` / トグル停止 / エラーまで連続実行。
- キー押下は既存 `KeybindHandler.pressKey` (`src/main/kotlin/xyz/inorganic/quickmenu/other/KeybindHandler.kt:12`) を再利用
  (この機構はキュー経由で次ティックに押下が反映されるため、非同期実行と相性が良い)。

ティックフックの追加箇所: `logic/KeybindManager.kt` の `onClientTick` 末尾に `MacroExecutor.advanceAll()`。

---

## 4. 実行方式: トグル式(active / passive の整理)

- 従来案の「active(常駐)/passive(一回のみ)」の区別は**廃止**。
  loop があればどちらも実現できるため、**全てのマクロ実行はトグル式**に統一する。
- **トグル式**:
  - 起動(ボタンクリック or キーバインド押下) → マクロが走り続ける。
  - もう一度同じ操作 → 即停止。
  - 一度きりの動作にしたい場合は `loop 1` を使えばよい。
- 現在の「1回だけ発火するだけ」のアクション群(コマンド/キー)は従来どおり。
  マクロアクションだけがトグル式の振る舞いを持つ。
- 実行中であることは**UIで可視化**する:
  - ボタン上に実行中インジケータ(例: ▶ マーク)を表示。
  - トグルで停止できること、今何が走っているかをユーザーが一目で把握できるようにする。

---

## 5. スクリプト言語 v1(テキスト形式)

1行1文、`#` から行末までコメント。

```
# 例: テレポート組み合わせ
cmd /tp ~ ~50 ~
wait 2s
key key.jump
chat テレポート完了!

# 例: 条件分岐
if item:minecraft:diamond_sword
    cmd /say 剣を持っています
else
    cmd /say 剣を持っていません
endif

# 例: ループ
loop 5
    key key.attack
    wait 3t
endloop
break        # ループを抜ける(if内などで使用)
exit         # マクロ即終了
```

| 構文 | 説明 |
|---|---|
| `run <マクロ名>` | 別のマクロを呼び出す(部品化) |
| `cmd <コマンド>` | コマンド実行(既存 `CommandActionData` と同じ) |
| `chat <テキスト>` | チャット送信(256文字にトランケート) |
| `key <keybind名>` | ゲーム内キーバインド押下(既存 `KeybindActionData` と同じ) |
| `set <名前> <値>` | 変数定義・再代入(`define` 相当) |
| `if <条件>` / `elif <条件>` / `else` / `endif` | 条件分岐(ネスト可) |
| `loop <回数>` / `endloop` | 回数指定ループ(ネスト可) |
| `loop` / `endloop` | 無限ループ(トグル停止 or `break` で抜ける) |
| `wait <数値>t` / `wait <数値>s` | 待機(ティック or 秒) |
| `label: <名前>` / `jump <名前>` | ラベル・GOTO(上級者向け) |
| `break` | ループ脱出 |
| `exit` | マクロ即終了 |

**条件式 v1**(拡張可能):
- `key:<キーバインド名>` … 押されているか
- `item:<レジストリID>` … 手持ち(メインハンド)にあるか
- `health > 5` / `health < 0.5` … 数値比較(変数と比較可)
- `!`(否定) / `&&`(AND) / `||`(OR)

---

## 6. ブロックエディタ案(ビジュアル / 初心者向け)

テキスト入力が難しいユーザー向けに、**Scratch 風のブロック型エディタ**を検討する。

### 6.1. ブロック種別 v1

| ブロック(表示は日本語) | 対応構文 | 設定項目 |
|---|---|---|
| コマンドを実行 | `cmd` | テキスト欄 |
| チャットを送る | `chat` | テキスト欄 |
| キーを押す | `key` | キーバインド選択ドロップダウン |
| マクロを実行 | `run` | マクロ名選択ドロップダウン |
| 待つ | `wait` | 数値 + 秒/ティック切替 |
| もし〜なら(ネスト可能) | `if` | 条件ビルダー(下記) |
| それ以外なら | `else` | — |
| 繰り返す(回数指定, ネスト可能) | `loop n` | 回数 |
| 繰り返す(無限, ネスト可能) | `loop` | — |
| 終了 | `exit` | — |

**条件ビルダー**(if ブロックの中): 「キーバインドが押されている」「アイテムを持っている」「体力が [数値] より大きい/小さい」などをドロップダウン + 数値欄で組み立て。

### 6.2. ブロック編集の操作(現実的な UX)

- 縦リスト形式。各ブロックに **↑/↓ 移動ボタン** と **削除ボタン**。
- ネストは **インデント表示** で表現。if / loop ブロックは「子ブロック」を畳む/開く。
- ブロック追加は「+ ブロックを追加」メニュー(種別選択)から。
- フルドラッグ&ドロップは plan.md §2.6(D&D 整備)と連動して将来対応。

### 6.3. ブロックとテキストの関係

- **正準形式はテキストスクリプト文字列**(保存・インポート/エクスポート・バージョン管理にそのまま使える)。
- ブロックエディタはパーサ結果(AST)をそのまま編集対象とし、保存時にテキストへ戻す。
  → **双方向変換**。ブロック編集とテキスト編集は同一データの2つのビュー。
- ブロックで表現できない文(`jump` / `elif` など)を含む場合は
  「このマクロにはブロックで扱えない要素があります → テキストエディタで開く」と案内。

---

## 7. ハイブリッド方針(両方実装)

- **テキストエディタ**: 全機能(`jump` / `elif` / 変数 / 高度な条件)。
- **ブロックエディタ**: 主要機能のサブセット。初心者はブロックから始め、必要になったらテキストに移行できる。
- マクロ作成 UI の入口で「ブロック(かんたん) / テキスト(くわしい)」を選択。
- 編集済みデータは共通の `MacroScriptModel` を通すため、どちらのエディタでも相互編集可。

---

## 8. 日本語対応

- エディタ UI のラベルは**既存 lang ファイル**(`assets/quickmenu/lang/ja_jp.json` 等)経由で翻訳。
- **ブロックエディタは完全日本語表示**(ブロック名・説明・条件ビルダー)。
- テキスト構文のキーワードは**ASCII 固定**(解析の安定性のため)。ただし:
  - コメント(`#` 以降)は日本語可
  - マクロ名・変数名は日本語可
  - `chat` の内容は日本語可
- 将来課題: 日本語キーワード(`もし` / `待つ` 等)のサポートは、構文解析エラーのリスクが高いため v1 では見送る。

---

## 9. 安全性

- **最大ステップ数 / インスタンス**(1ティックあたり上限)
- **最大ループ回数** の config 設定
- **最大実実行時間**(例: 10秒超で強制停止 + チャット警告)— 無限ループ死亡対策
- **緊急停止キー**(Ctrl+C 相当、専用キーバインド追加)— 全実行中インスタンスを破棄
- エラーは `try/catch` でチャットに1行表示して当該インスタンスのみ停止。クラッシュさせない。

---

## 10. データ直列化

- `Serialization.kt` の when (`src/main/kotlin/xyz/inorganic/quickmenu/data/Serialization.kt:35-39`) に `"macro" -> MacroActionData(script)` を追加。
- スクリプトは複数行文字列。JSON 文字列内の改行(`\n`)をそのまま許容し、既存の `[type, value]` 2要素形式を維持する。
- トグル実行状態(実行中か)は**永続化しない**(実行時のみ)。
- インポート/エクスポート(既存 Base64 機構)はテキスト形式のまま通る。

---

## 11. 実装ファイル対応表

| ファイル | 内容 |
|---|---|
| `data/command_actions/MacroActionData.kt` | 新 `ActionData` サブクラス(`type = "macro"`) |
| `macros/MacroParser.kt` | テキスト → 文リスト(AST)変換。構文エラー(行番号付き)報告 |
| `macros/MacroInterpreter.kt` | 文の実行。ラベル / if / loop / break / exit 制御 |
| `macros/MacroExecutor.kt` | インスタンス管理 + ティック前進 + 緊急停止 + トグル制御 |
| `macros/MacroScriptModel.kt` | ブロック/テキスト共通モデル(双方向変換の中核) |
| `logic/KeybindManager.kt` | tick 末尾に `MacroExecutor.advanceAll()` 追加 |
| `ui/TextMacroEditorUI.kt` | テキストエディタ(行番号・エラー表示・▶ Run) |
| `ui/BlockMacroEditorUI.kt` | ブロックエディタ(縦リスト + ネスト + ブロック追加) |
| `ui/ActionEditorUI.kt` | アクションリストに「マクロ」種別を追加(入口) |
| `data/Serialization.kt` | `"macro"` 型のデシリアライズ追加 |

---

## 12. 実装フェーズ(提案)

```
Phase A: 基盤
  - MacroParser / MacroInterpreter / MacroExecutor(テキスト実行)
  - トグル実行 + 実行中インジケータ
  - 緊急停止・安全対策
  - テキストエディタ(最小)
Phase B: ブロックエディタ
  - MacroScriptModel の双方向変換
  - ブロック種別 v1 + 条件ビルダー
Phase C: 仕上げ
  - 日本語 UI ラベル
  - インポート/エクスポート動作確認
  - ドラッグ&ドロップ(plan §2.6 と連動)
```
